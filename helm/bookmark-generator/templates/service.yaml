apiVersion: v1
kind: Service
metadata:
  name: {{ include "bookmark-generator.fullname" . }}
  labels:
    {{- include "bookmark-generator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bookmark-generator.selectorLabels" . | nindent 4 }}
